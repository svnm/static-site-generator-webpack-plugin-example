//ideally we can replace this with some sort of
//react-router to array. (package exists on npm)
//but for simple purposes manually writing the routes is fine
module.exports = {
  routes: [
    '/',
    '/jsing',
    '/styling',
    '/contact'
  ]
}
